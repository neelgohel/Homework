<%= form_for (@booking) do |f| %>
<% if @booking.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this cleaner from being saved:</h2>

    <ul>
    <% @booking.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<div class="field">
  <%= f.label :City %>
  <%= f.select :city_id,options_from_collection_for_select(@city,'id','city_name') %>
</div>

<div class="field">
  <%= f.label :data_and_time %>
  <%= f.datetime_select :datetime %>
</div>


<div class="action">
  <%= f.submit %>
</div>
<% end %>
