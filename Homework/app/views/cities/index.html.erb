<%= button_to "Admin",admins_path,method:'get'%>
<%= button_to :"Add City",new_city_path,method:'get' %><br/>
<% @cities.each do |city| %>
  <%= city.city_name %> <%= link_to :edit,edit_city_path(city.id) %> <%= link_to :show,city_path(city.id) %> <br/>
<% end %>
